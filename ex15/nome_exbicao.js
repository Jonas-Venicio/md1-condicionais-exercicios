const primeiroNome = "Jonas";
const sobreNome = "";
const apelido = "";

if (apelido) {
    console.log(apelido);
}
else if (sobreNome) {
    console.log(primeiroNome + " " + sobreNome);
}
else {
    console.log(primeiroNome);
}